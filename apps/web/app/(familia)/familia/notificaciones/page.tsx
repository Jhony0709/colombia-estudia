/**
 * Notificaciones del acudiente (Fase C, 23/9): la misma página que la del estudiante,
 * bajo la barra de `/familia`. `listNotifications` filtra por persona, no por área.
 */

export { default, metadata } from '@/app/(student)/aprender/notificaciones/page';
